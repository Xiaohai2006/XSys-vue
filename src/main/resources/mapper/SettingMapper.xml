<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhkj.xsys.mapper.SettingMapper">

    <select id="getValue" resultType="string">
        SELECT s_value FROM sys_setting WHERE s_key = #{key}
    </select>

    <insert id="upsert">
        INSERT INTO sys_setting (s_key, s_value) VALUES (#{key}, #{value})
        ON DUPLICATE KEY UPDATE s_value = VALUES(s_value)
    </insert>
</mapper>
